git是分布式的版本管理工具,帽籧lone出来的版本都是和原来的版本一模一样,并且拥有整个版本的历史记录

git保存的所有版本中,的每一个版本中的文件都是对应的一个快照索引,git 中的数据就相当于一个快照流的存在

git的操作大部分都只是本地的操作,没有经过网络连接服务器,每个本地git仓库包含了该版本之前的所有日志

git的存储前经过计算机哈希校验保证文件的正确性,git数据库中文件的保存都是以文件的内容的哈希值来索引的
:
